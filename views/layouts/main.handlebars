<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Stickers{{#if title}} | {{title}}{{/if}}</title>
  </head>
  <body>

    <header>
      <h1>Stickers</h1>
    </header>

    <nav>
        <a class="button" href="/">My Stickers</a>
        <a class="button" href="/stickerForm">New Sticker</a>
    </nav>

    <div class="container">
      {{{body}}}
    </div>

  </body>
</html>
